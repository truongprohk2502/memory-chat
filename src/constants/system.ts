const LANGUAGES = [
  {
    value: 'en',
    i18nLabelPath: 'setting.options.system.options.language.options.english',
  },
  {
    value: 'vi',
    i18nLabelPath: 'setting.options.system.options.language.options.vietnamese',
  },
];

const DEFAULT_LANGUAGE = 'en';

const DEFAULT_NOTIFICATION_SETTING = {
  soundNotification: true,
  desktopNotification: true,
};

export { LANGUAGES, DEFAULT_LANGUAGE, DEFAULT_NOTIFICATION_SETTING };
